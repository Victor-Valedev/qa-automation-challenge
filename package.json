{
  "scripts": {
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:report": "npx mochawesome-merge mochawesome-report/*.json > mochawesome.json && npx mochawesome-report-generator mochawesome.json -o mochawesome-html",
    "cy:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^15.5.0",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.3.0"
  }
}
